<h1>To Do List</h1>

<table>
  <tr>
    <th>Task</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= link_to task.title, task %></td>
    <td><%= button_to 'Finished!' task, method: :delete, data: {confirm: 'You sure?'} %></td> 
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add another?', new_topic_path %>
